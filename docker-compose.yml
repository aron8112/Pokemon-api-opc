version: '3.8'
services:
  db:
    image: mysql
    cap_add:
      - SYS_NICE
    restart: always
    environment:
      - MYSQL_DATABASE=pokemon
      - MYSQL_USER=node
      - MYSQL_PASSWORD=nodeapp
      - MYSQL_ROOT_PASSWORD=root
    ports:
      - '3313:3313'
    volumes:
      - db:/var/lib/mysql
volumes:
  db:
    driver: local